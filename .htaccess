RewriteEngine On
RewriteBase /

# Remove .html extension for /confirmed
RewriteRule ^confirmed$ confirmed.html [L,NC]

# Optional: Remove .html from all pages
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_FILENAME}\.html -f
RewriteRule ^(.*)$ $1.html [L]